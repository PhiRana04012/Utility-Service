# 🚀 Быстрый старт - Utility Service

## Запуск всего проекта (Backend + Frontend + БД)

### 1. Из корня проекта выполните:

```bash
docker-compose up -d
```

### 2. Дождитесь запуска всех сервисов:

```bash
docker-compose ps
```

Должны быть запущены:
- ✅ `utility-db` (MySQL)
- ✅ `utility-backend` (API)
- ✅ `utility-frontend` (Vue приложение)

### 3. Откройте в браузере:

**Frontend:** http://localhost:8080

**Backend API:** http://localhost:3001/issues

**База данных:** localhost:3307 (если нужен прямой доступ)

### 4. Выполните миграции БД (если нужно):

```bash
docker exec -it utility-backend sh
npx knex migrate:latest
exit
```

## Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Пересборка после изменений
docker-compose up -d --build

# Остановка с удалением данных БД
docker-compose down -v
```

## Что запускается:

- **MySQL** на порту 3307 (внешний) / 3306 (внутри контейнера)
- **Backend API** на порту 3001  
- **Frontend** на порту 8080 (внешний) / 80 (внутри контейнера)

Подробная документация: [README.DOCKER.md](./README.DOCKER.md)

